import 'package:flutter/material.dart';

//the navbar
//TODO: Customize container according to the demo
class NavigationBar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}